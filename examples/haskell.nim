; import examples/lambda.nim
; (#n +)
  ----------
  (λ x -> (!#n + x))

; (+ #n)
  ----------
  (λ x -> (x + !#n))

; (#n *)
  ----------
  (λ x -> (!#n * x))

; (* #n)
  ----------
  (λ x -> (x * !#n))

; (#a)
  ----------
  #a

; o f g x = f (g x)
; math (o (+ 5) (1 +) 6)
