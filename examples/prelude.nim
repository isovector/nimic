((macro (defined ; #b) #b)
  ; ( (rassoc 1 ;)
    ; macro (#a => #b) (macro #a #b)
    ; (() ; #a) => #a
    ; rassoc 2 =>
    ; import #module => bash (cat #module)
    ; import examples/math.nim
    ; math (1 + 2)
  )
)
